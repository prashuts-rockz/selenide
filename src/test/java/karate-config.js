function fn() {
/*    var testData = karate.readAsCsv('classpath:testData.csv');

    if (!testData || testData.length === 0) {
        karate.log('❌ ERROR: testData.csv is empty or not loaded!');
    } else {
        karate.log('✅ Loaded CSV Data:', testData);
    }

    return { testData: testData };*/
}